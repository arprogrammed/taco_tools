import { Inter, Grandstander } from 'next/font/google';
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});
 
export const grand = Grandstander({
  subsets: ['latin'],
  display: 'swap',
});